#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    bool exist(vector<vector<char>>& board, string word) {
        // 如果当前的序列 满足word 前部分顺序 则进行回溯 方向扩展
        
        
    }
};
